class TwoPointer{

    public void variableSlidingWindowTemplate(int num[]){
        int left = 0;
        for(int right=0; right<num.length; right++){

            //expand the window by expanding the right

            if( 
                // window is invalid, condition is not met
                ){
                //shrink window from left
                left++;
            }

            // update answer if valid
        }
    }
}